# Prompt 13 Report Files

This directory contains the final simulation-convergence study, compared directly with the Prompt 12 result.

- `CONVERGENCE_REPORT.md` — readable target review and handoff
- `prompt12_report.json` and `final_report.json` — paired machine-readable aggregates
- `target_status.csv` — PASS/FAIL evidence for every locked target
- `system_before_after.csv` and `bartender_before_after.csv` — paired comparisons
- `card_target_review.csv` and `customer_target_review.csv` — content watch lists
- `drink_price_change.csv` — the single Cellar Select adjustment
- `final_*.csv` — complete final bartender, drink, customer, matchup, length, summary, and switch results

All formal targets passed. Three cards remain a deck-diversity warning within the single deterministic heuristic recipe, so human deck telemetry should be collected during public playtesting.
